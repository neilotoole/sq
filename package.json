{
  "name": "sq-devtools",
  "private": true,
  "description": "Root dev tooling for the sq repository: dprint (repo-wide formatting) and Biome (site JS lint).",
  "engines": {
    "bun": ">=1.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "dprint": "^0.55.0"
  }
}
